#!/bin/bash
git config --global credential.helper store
git config --global user.email "rob@robsoft.net"
git config --global user.name "Rob Uttley"
echo "edit first_fetch.sh accordingly and then run it"
